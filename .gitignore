
# Local API keys and config
.env
web/config.local.js
SRT/Groq.md

# Private session data (user recordings, transcripts, notes)
# All per-session products stay on user's local machine only.
sessions/

# Audio / video source files (may contain private recordings)
*.m4a
*.mp3
*.wav
*.mp4
*.mov
*.webm
*.flac
*.ogg
*.mpeg
*.mpga
*.mkv

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Editor / backup files
*.bak
*.swp
*.swo
.DS_Store

# Personal Claude Code settings (may contain user-specific paths/prefs)
.claude/settings.local.json

# ── 製作品/過程/錄音(嚴禁上推,只留本機)──
# 每篇筆記的處理資料夾(音檔、SRT、cleaned.md、HTML、照片)
ClaudeDesign/
# Step 5 部署產物(生成的多頁 HTML + 壓縮圖 + firebase 快取)
scripts/publish/
# 根目錄的成品檔(逐字稿/翻譯/HTML 等交付物)
/claude_*.srt
/claude_*.md
/claude_*.html
/claude_*.txt
# 一次性/過程腳本與草稿(非可重用工具)
/process_claude_audio.py
/temp_srt_to_plain.py
/fix_cleaned.py
/speaker*_raw.txt
# 其他與本專案規範無關的本機資料夾
/GENAI/
/claudeCode/
# 個人檔案/測試殘渣集中區(2026-06 盤點引入:不屬專案的檔案搬此,不上推)
/_local/
# 根目錄測試殘渣
/test*.py
/test*.txt
# 轉錄中間產物(YouTube VTT 字幕,屬 session 產物)
*.vtt

# ── Step 1-6 工作產物 file-level glob(任何位置都不上推)──
# 不論放在 sessions/、ClaudeDesign/、MCP5/、或未來 MCP6/、koshi-2/... 任何 workdir,
# 都用 file-level pattern 一網打盡,避免「新建一個工作資料夾忘了加 .gitignore」。
# Step 1 — Groq 轉錄產物
**/transcript.srt
**/source.srt
# Step 2 — Phase A/B 清理稿
**/cleaned.md
**/cleaned.srt
**/transcript.cleaned.srt
# Step 3 — 專名詞補充
**/enhanced.md
# Step 4 — 立場置入好學生筆記
**/notes_*.md
# Build / metadata / 個人校正 / engine 中繼 marker
**/toc.json
**/metadata.json
**/corrections.json
**/_build/
**/_kc_build/
**/_translate/
**/_workdir/
**/_local_backup/
**/.phase_*_pending.json
**/.step_*_pending.json
# 個別專案 workdir(對應的截圖/原始照片 raw input;新增 workdir 時請依此格式補一行)
MCP5/

# ── 過程文件/對外文宣(非專案規範,只留本機)──
# Newsletter / 能力宣傳稿等交付物,任何位置皆不上推
# (docs/origin-story.md 等規範文件不受影響)
**/*newsletter*.md

# 派生/執行期產物留本機(可由 master + scripts 重生;見 memory outward-docs 原則)
build/
.worktrees/
